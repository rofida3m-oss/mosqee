# ✅ دليل الاختبار الشامل - 12 يناير 2026

## 🚀 البدء السريع:

### 1️⃣ في Terminal الأول (الخادم):
```bash
cd c:\Users\kali\Desktop\mosqee\server
npm run dev
# انتظر حتى تري:
# ✅ Database initialized
# 🚀 Server running at http://localhost:5000
```

### 2️⃣ في Terminal الثاني (التطبيق):
```bash
cd c:\Users\kali\Desktop\mosqee
npm run dev
# انتظر حتى تري:
# ➜  Local:   http://localhost:3000/
```

---

## 🔧 إذا حصل خطأ في قاعدة البيانات:

**في console المتصفح (F12):**
```javascript
// امسح قاعدة البيانات القديمة
localStorage.removeItem('jami_sqlite_db');

// أعد التحميل
location.reload();
```

ستُعاد قاعدة البيانات بالجداول الجديدة تلقائياً ✅

---

## 🧪 السيناريوهات الاختبارية:

### **السيناريو 1: اختبر الإعجابات**

#### الخطوة 1: تسجيل الدخول
```
1. افتح http://localhost:3000
2. أدخل رقم الهاتف: 0100000001
3. اختر "مستخدم عادي" 
4. اضغط "دخول"
```

#### الخطوة 2: أنشئ منشور
```
1. اضغط على "نشر"
2. اكتب: "اختبار الإعجابات"
3. اضغط "نشر"
```

#### الخطوة 3: اختبر الإعجاب من نفس الجهاز
```
1. اضغط على ♥️ (قلب أحمر)
2. يجب أن ترى:
   ✅ عدد الإعجابات يزيد من 0 إلى 1 فوراً
   ✅ رسالة "تم الإعجاب بالمنشور"
   ✅ القلب يصير أحمر
```

#### الخطوة 4: اختبر المزامنة
```
1. افتح متصفح جديد أو Incognito
2. اذهب لـ http://localhost:3000
3. سجّل الدخول بنفس رقم الهاتف
4. ستشوف المنشور مع عدد الإعجابات = 1 ✨

إذا لم تشوفه:
- أعد التحميل (F5)
- انتظر ثوان معدودة (auto-sync)
```

---

### **السيناريو 2: اختبر التعليقات**

#### الخطوة 1: أضف تعليق من الجهاز الأول
```
1. اضغط على "تعليق" (💬)
2. اكتب في صندوق التعليق: "تعليق اختباري"
3. اضغط أيقونة الإرسال (➤)
4. التعليق يظهر فوراً تحت المنشور ✅
```

#### الخطوة 2: شاهد التعليق من الجهاز الثاني
```
1. في الجهاز الثاني أعد التحميل
2. اضغط "تعليق"
3. يجب أن تشوف التعليق الذي كتبته 🎉
```

#### الخطوة 3: احذف التعليق
```
1. في الجهاز الأول، شوف تعليقك
2. اضغط عليه (سيظهر X)
3. اضغط X
4. التعليق يختفي من كل الأجهزة ✅
```

---

### **السيناريو 3: اختبر المشاركات**

#### الخطوة 1: شارك المنشور
```
1. اضغط "مشاركة"
2. رسالة تقول "تم المشاركة" تظهر ✅
3. عدد المشاركات يزيد
```

#### الخطوة 2: شاهد من الجهاز الثاني
```
1. أعد التحميل على الجهاز الثاني
2. عدد المشاركات يكون زاد 🎉
```

---

### **السيناريو 4: اختبر تعديل المنشور**

#### الخطوة 1: عدّل المنشور
```
1. اضغط ⋮ (ثلاث نقاط) على منشورك
2. اختر "تعديل"
3. غيّر النص إلى: "منشور معدّل"
4. اضغط "حفظ"
5. المنشور يتحدث فوراً ✅
```

#### الخطوة 2: شاهد التعديل من الجهاز الثاني
```
1. أعد التحميل
2. يجب أن تشوف النص الجديد 🎉
```

---

### **السيناريو 5: اختبر حذف المنشور**

#### الخطوة 1: احذف المنشور
```
1. اضغط ⋮ على منشورك
2. اختر "حذف"
3. اضغط تأكيد
4. المنشور يختفي من الجهاز الأول فوراً ✅
```

#### الخطوة 2: تحقق من الجهاز الثاني
```
1. أعد التحميل
2. المنشور يكون اختفى من هنا أيضاً 🎉
```

---

### **السيناريو 6: اختبر Mode بدون خادم (Offline)**

#### الخطوة 1: أوقف الخادم
```
1. في Terminal الخادم، اضغط Ctrl+C
2. الخادم ينغلق
```

#### الخطوة 2: جرّب الإعجابات
```
1. في الجهاز الأول، اضغط إعجاب على منشور
2. يجب أن يعمل محلياً بدون الخادم ✅
3. لا يوجد error message
```

#### الخطوة 3: شغّل الخادم مجدداً
```
1. في Terminal الخادم:
   npm run dev
2. كل الآن عند الجهاز الثاني ستتزامن 🎉
```

---

## ✅ قائمة التحقق النهائية:

| الميزة | الاختبار | النتيجة |
|-------|--------|--------|
| الإعجابات | اضغط ♥️ وشاهد يزيد | ✅ / ❌ |
| التعليقات | اكتب وأرسل | ✅ / ❌ |
| حذف التعليق | اضغط X | ✅ / ❌ |
| المشاركات | اضغط مشاركة | ✅ / ❌ |
| التعديل | عدّل المنشور | ✅ / ❌ |
| الحذف | احذف المنشور | ✅ / ❌ |
| المزامنة | جهاز ثاني | ✅ / ❌ |
| Offline | بدون خادم | ✅ / ❌ |

---

## 🐛 إذا واجهت مشكلة:

### المشكلة: الإعجاب لا يعمل
**الحل:**
1. افتح Console (F12)
2. شوف الأخطاء
3. تأكد الخادم يعمل (localhost:5000 يستجيب)
4. امسح localStorage وأعد التحميل

### المشكلة: التعليق لا يظهر
**الحل:**
1. شوف Console للأخطاء
2. تأكد من عدم ترك حقل التعليق فارغ
3. أعد التحميل

### المشكلة: البيانات لا تتزامن
**الحل:**
1. انتظر 30 ثانية (auto-sync)
2. أعد تحميل الصفحة (F5)
3. تأكد أن الخادم يعمل
4. فتّش Network tab في DevTools

### المشكلة: Error في قاعدة البيانات
**الحل:**
```javascript
// في Console:
localStorage.clear();
location.reload();
```

---

## 📊 معلومات مفيدة:

### كيفية الوصول للـ Database:
```
المحلي (Browser): localStorage['jami_sqlite_db']
الخادم: server/data/mosqee.db (SQLite)
```

### كيفية رؤية الأخطاء:
```
اضغط F12 > Console
اضغط F12 > Network (لمتابعة API calls)
```

### الـ Endpoints المهمة:
```
http://localhost:5000/api/posts           - كل المنشورات
http://localhost:5000/api/health          - هل الخادم يعمل
```

---

## 🎉 النتيجة النهائية المتوقعة:

بعد اتباع جميع السيناريوهات:
- ✅ جميع الإعجابات تعمل
- ✅ جميع التعليقات تعمل
- ✅ جميع المشاركات تعمل
- ✅ الحذف والتعديل يعملان
- ✅ المزامنة بين الأجهزة تعمل
- ✅ النظام يعمل بدون خادم

---

**آخر تحديث:** 12 يناير 2026  
**الحالة:** جاهز للاختبار الكامل ✅
