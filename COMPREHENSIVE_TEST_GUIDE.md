# 🧪 دليل الاختبار الشامل بعد الإصلاحات

**التاريخ:** 12 يناير 2026  
**الحالة:** جاهز للاختبار الفوري

---

## ✅ ما تم إصلاحه:

1. ✅ **توافق البيانات:** userId الآن يُرسل بشكل صحيح
2. ✅ **التعليقات:** توافق كامل بين Frontend و Backend
3. ✅ **الأوقات:** إضافة timestamps لجميع التفاعلات
4. ✅ **الحفظ:** saveDB() يُنفذ بعد كل عملية

---

## 🚀 خطوات الاختبار الفوري:

### المرحلة الأولى: التحضير
```bash
# Terminal 1 - الخادم (يجب أن يكون مشغلاً)
cd c:\Users\kali\Desktop\mosqee\server
npm run dev
# Output يجب أن يكون:
# ✅ Database initialized
# 🚀 Server running at http://localhost:5000
# 📊 API endpoints ready
```

```bash
# Terminal 2 - التطبيق (يجب أن يكون مشغلاً)
cd c:\Users\kali\Desktop\mosqee
npm run dev
# Output يجب أن يكون:
# ✅ VITE ready in ... ms
# ➜ Local: http://localhost:3000
```

---

### المرحلة الثانية: الاختبار الأساسي

#### **🔑 خطوة 1: تسجيل الدخول**
```
1. افتح http://localhost:3000
2. أدخل رقم هاتف: 0100000000
3. أدخل الكود (OTP): 1234
4. اختر: إمام أو مسؤول
5. اضغط "دخول"
```

**المتوقع:** ✅
- التطبيق ينقلك إلى الصفحة الرئيسية
- اسمك يظهر في الأعلى
- يجب أن ترى "جاهز للاستخدام"

---

#### **📝 خطوة 2: إنشاء منشور**
```
1. من الصفحة الرئيسية (Feed)
2. اضغط على حقل "اكتب ما في بالك"
3. اكتب: "منشور اختبار الإعجابات والتعليقات"
4. اضغط "نشر"
```

**المتوقع:** ✅
- المنشور يظهر فوراً في الصفحة
- العداد: 0 إعجابات، 0 تعليقات
- محفوظ في قاعدة البيانات

**تحقق من Console (F12):**
```javascript
// يجب أن ترى:
Database saved successfully
```

---

#### **❤️ خطوة 3: اختبار الإعجاب**
```
1. من المنشور الذي أنشأته
2. اضغط على قلب الإعجاب ❤️
```

**المتوقع:** ✅
- القلب يتغير لونه فوراً (أحمر)
- العداد يزيد: 0 → 1
- لا توجد أي أخطاء في Console

**تحقق من قاعدة البيانات:**
```bash
# في PowerShell:
sqlite3 c:\Users\kali\Desktop\mosqee\server\data\mosqee.db
> SELECT COUNT(*) FROM post_likes;
# يجب أن يُرجع: 1
```

---

#### **💬 خطوة 4: اختبار التعليقات**
```
1. من المنشور نفسه
2. اضغط على حقل التعليق
3. اكتب: "تعليق اختبار"
4. اضغط "أضف تعليق"
```

**المتوقع:** ✅
- التعليق يظهر فوراً تحت المنشور
- يظهر اسمك (الذي سجلت به)
- العداد يتحدث: 0 → 1
- **بيانات التعليق الصحيحة:**
  - userId: ✅ معرف فعلي (ليس اسم)
  - userName: ✅ اسم ظاهر
  - content: ✅ النص
  - createdAt: ✅ التاريخ والوقت

**تحقق من Console:**
```javascript
// يجب أن ترى:
Server sync failed: ... (طبيعي إذا كان الخادم لم يرسل response بشكل كامل)
// لكن التعليق محفوظ محلياً ✅
```

---

#### **📤 خطوة 5: اختبار المشاركة**
```
1. من المنشور
2. اضغط على "مشاركة" 📤
```

**المتوقع:** ✅
- رسالة: "تم المشاركة بنجاح"
- إشعار أخضر يظهر
- العداد للمشاركات يزيد (إذا كان موجوداً)

---

#### **🗑️ خطوة 6: اختبار حذف التعليق**
```
1. على التعليق الذي أضفته
2. اضغط على الأيقونة ⋮ (الثلاث نقاط)
3. اختر "حذف تعليق"
4. أكّد الحذف
```

**المتوقع:** ✅
- التعليق يختفي فوراً من الواجهة
- العداد ينقص: 1 → 0
- محذوف من قاعدة البيانات

---

### المرحلة الثالثة: اختبار المزامنة بين الأجهزة

#### **🔄 خطوة 7: المزامنة الفوري**
```
1. من الجهاز الأول (ما زلت عليه)
   - اضغط إعجاب على المنشور

2. اختر نافذة Incognito جديدة (أو متصفح مختلف)
   - افتح http://localhost:3000
   - سجّل دخول بنفس الرقم: 0100000000
   - اختر نفس الدور

3. انظر إلى المنشور نفسه
```

**المتوقع:** ✅
- المنشور يظهر على كلا الجهازين
- الإعجابات والتعليقات متطابقة
- **بعد 30 ثانية،** جميع البيانات متزامنة

**الجدول الزمني:**
- ⏱️ 0 ثانية: إعجاب فوري على الجهاز الأول
- ⏱️ 0 ثانية: حفظ محلي على الجهاز الأول
- ⏱️ فوراً: محاولة إرسال للخادم (Background)
- ⏱️ 30 ثانية: الجهاز الثاني يجلب التحديثات

---

### المرحلة الرابعة: اختبار الأخطاء والاستعادة

#### **⚠️ خطوة 8: اختبار بدون خادم (اختياري)**
```
1. أوقف الخادم (اضغط Ctrl+C في Terminal 1)
2. من التطبيق
3. أضف إعجاب جديد على منشور
4. أضف تعليق جديد
5. شارك المنشور
```

**المتوقع:** ✅
- جميع العمليات تعمل **بدون خطأ**
- البيانات تُحفظ محلياً
- رسالة خطأ صامتة (لا تزعج المستخدم)

**ثم أعد تشغيل الخادم:**
```
6. ابدأ الخادم مجدداً
7. انتظر 30 ثانية
8. يجب أن تُزامن جميع البيانات
```

---

## 🔍 عمليات التحقق:

### في Browser Console (F12):
```javascript
// 1. تحقق من localStorage
localStorage.getItem('jami_user_id')
// يجب أن يرجع: "u_XXXXX" أو "admin_XXXXX"

// 2. تحقق من قاعدة البيانات المحلية
localStorage.getItem('jami_sqlite_db')
// يجب أن يرجع: بيانات مشفرة (بداية: [...]

// 3. تحقق من الأخطاء
console.error
// لا يجب أن تكون هناك أخطاء متعلقة بـ undefined أو null
```

### في Server logs:
```bash
# يجب أن ترى:
✅ Database initialized
🚀 Server running at http://localhost:5000
📊 API endpoints ready

# عند كل طلب API:
POST /api/posts/:postId/comments 200 OK
POST /api/posts/:postId/like 200 OK
DELETE /api/comments/:commentId 200 OK
```

### في قاعدة البيانات (SQLite):
```bash
sqlite3 c:\Users\kali\Desktop\mosqee\server\data\mosqee.db

# 1. عدد الإعجابات
SELECT COUNT(*) FROM post_likes;
# المتوقع: > 0

# 2. عدد التعليقات
SELECT COUNT(*) FROM post_comments;
# المتوقع: > 0

# 3. تفاصيل التعليق
SELECT * FROM post_comments LIMIT 1;
# يجب أن ترى: id, post_id, user_id, user_name, content, likes, created_at

# 4. تفاصيل الإعجاب
SELECT * FROM post_likes LIMIT 1;
# يجب أن ترى: id, post_id, user_id, created_at
```

---

## ❌ الأخطاء الشائعة والحلول:

| الخطأ | السبب | الحل |
|------|------|------|
| `Cannot read property 'id' of undefined` | currentUser غير محدد | تحقق من تسجيل الدخول |
| `TypeError: dbQueries.addPostComment is not a function` | dbService لم يُحمّل | أعد تحميل الصفحة (F5) |
| `UNIQUE constraint failed` | إعجاب مكرر | طبيعي - لا يمكن الإعجاب مرتين |
| `Cannot connect to server` | الخادم معطل | تحقق من `npm run dev` في Terminal 1 |
| `Post not found in database` | خطأ في ID | جرّب تحديث الصفحة |

---

## 📊 الملخص:

**بعد الإصلاحات، يجب أن يعمل:**
- ✅ إنشاء المنشورات بدون مشاكل
- ✅ الإعجاب والـ Unlike بسرعة
- ✅ التعليقات مع بيانات صحيحة
- ✅ حذف التعليقات
- ✅ المشاركات
- ✅ المزامنة بين الأجهزة (30 ثانية)
- ✅ العمل بدون خادم (Offline)
- ✅ حفظ دائم على الخادم

**إذا حدثت مشاكل:**
1. افتح F12 (Developer Tools)
2. تحقق من Console للأخطاء
3. راجع DIAGNOSTIC_REPORT.md
4. تحقق من أن كل Terminals تعمل

---

**كل شيء جاهز الآن! 🚀✨**

اختبر وأخبرني بالنتائج!
