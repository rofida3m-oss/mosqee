# ⚡ اختبار سريع - تحقق من كل شيء في 5 دقائق

---

## 1️⃣ التحقق من الخادم

```bash
# اختبر في PowerShell:
$response = Invoke-WebRequest -Uri http://localhost:5000/api/posts -Method GET -UseBasicParsing
$response.StatusCode
# يجب أن يرجع: 200
```

**النتيجة:** 
- ✅ 200 = الخادم يعمل
- ❌ غير ذلك = أعد تشغيل الخادم

---

## 2️⃣ التحقق من التطبيق

```bash
# افتح:
http://localhost:3000

# يجب أن ترى:
- صفحة تسجيل الدخول
- حقول الإدخال واضحة
- الأزرار تستجيب للضغط
```

**النتيجة:**
- ✅ تظهر = التطبيق يعمل
- ❌ أخطاء = F12 و Console

---

## 3️⃣ اختبر تسجيل الدخول

```
1. رقم الهاتف: 0100000000
2. كود OTP: 1234
3. الدور: إمام
4. اضغط دخول
```

**النتيجة:**
- ✅ ينقلك إلى الصفحة الرئيسية
- ✅ يظهر اسمك في الأعلى
- ❌ خطأ = راجع Console (F12)

---

## 4️⃣ اختبر إنشاء منشور

```
1. في الصفحة الرئيسية
2. اكتب: "اختبار"
3. اضغط "نشر"
```

**النتيجة:**
- ✅ المنشور يظهر فوراً
- ✅ عداد الإعجابات = 0
- ✅ عداد التعليقات = 0
- ❌ خطأ = راجع Console

---

## 5️⃣ اختبر الإعجاب

```
1. على المنشور
2. اضغط قلب الإعجاب ❤️
```

**النتيجة:**
- ✅ القلب يتحول لون أحمر
- ✅ العداد 0 → 1
- ✅ بدون تأخير

---

## 6️⃣ اختبر التعليق

```
1. على المنشور
2. اضغط على حقل التعليق
3. اكتب: "تعليق اختبار"
4. اضغط "أضف تعليق"
```

**النتيجة:**
- ✅ التعليق يظهر فوراً
- ✅ يظهر اسمك
- ✅ العداد 0 → 1
- ✅ بدون تأخير

---

## 7️⃣ اختبر الحذف

```
1. على التعليق الذي أضفته
2. اضغط ⋮ (الثلاث نقاط)
3. اختر "حذف"
```

**النتيجة:**
- ✅ التعليق يختفي فوراً
- ✅ العداد 1 → 0
- ✅ بدون أخطاء

---

## 8️⃣ اختبر المزامنة

```
الجهاز الأول:
1. أضف إعجاب على المنشور

الجهاز الثاني (متصفح مختلف أو Incognito):
1. افتح http://localhost:3000
2. سجّل دخول (نفس الرقم)
3. افتح الصفحة الرئيسية
4. انظر إلى المنشور
```

**النتيجة:**
- ⏱️ 0-5 ثوانٍ: قد لا ترى الإعجاب بعد
- ⏱️ 30 ثانية: يجب أن ترى الإعجاب
- ✅ المزامنة تعمل

---

## 🔍 فحص Console (F12)

```javascript
// اضغط F12 → Console

// يجب أن ترى:
✅ "Database saved successfully"  // عند حفظ
✅ "Server sync failed: ..."     // طبيعي إذا كان الخادم لم يرسل
❌ "Cannot read property..."     // خطأ - راجع الملفات
❌ "TypeError:"                  // خطأ - راجع الملفات
```

---

## 📊 فحص قاعدة البيانات

```bash
# في PowerShell:
cd c:\Users\kali\Desktop\mosqee\server
sqlite3 data/mosqee.db

# اكتب:
SELECT COUNT(*) FROM post_likes;
# يجب أن يرجع: 1 أو أكثر

SELECT COUNT(*) FROM post_comments;
# يجب أن يرجع: 1 أو أكثر

SELECT * FROM post_comments LIMIT 1;
# يجب أن ترى:
# id | post_id | user_id | user_name | content | likes | created_at
```

---

## ✅ قائمة التحقق السريعة

```
□ الخادم يعمل على 5000
□ التطبيق يعمل على 3000
□ تسجيل الدخول يعمل
□ إنشاء المنشور يعمل
□ الإعجاب يعمل (فوري)
□ التعليق يعمل (فوري)
□ حذف التعليق يعمل
□ المشاركة تعمل
□ لا توجد أخطاء في Console
□ قاعدة البيانات تحفظ البيانات
```

**إذا جميع العلامات ✅:** 
## 🎉 كل شيء يعمل بشكل مثالي!

---

## ❌ إذا حدثت مشاكل:

| المشكلة | الحل |
|--------|------|
| الخادم لا يعمل | اكتب `npm run dev` في folder `server` |
| التطبيق لا يعمل | اكتب `npm run dev` في folder الرئيسي |
| أخطاء في Console | اقرأ الخطأ بدقة وراجع DIAGNOSTIC_REPORT.md |
| البيانات لا تُحفظ | تحقق من localStorage (F12 > Application) |
| لا تحدث مزامنة | انتظر 30 ثانية - المزامنة تحدث كل 30 ثانية |

---

**انتهى الاختبار السريع! ⏱️ 5 دقائق**

كل شيء يعمل؟ رائع! 🚀✨
