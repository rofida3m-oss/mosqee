// ضع هذا الكود في Console (F12) من المتصفح لحل المشكلة:

// 1. حذف قاعدة البيانات القديمة
localStorage.removeItem('jami_sqlite_db');

// 2. أعد تحميل الصفحة
location.reload();

// هذا سيعيد إنشاء قاعدة البيانات بالجداول الجديدة ✅
