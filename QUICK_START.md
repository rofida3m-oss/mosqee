## 🚀 ابدأ هنا!

### ✅ ما تم إضافته:

1. **Backend Server** (`/server`)
   - Express.js API
   - SQLite Database
   - جميع الـ Endpoints المطلوبة

2. **API Service** (`/services/apiService.ts`)
   - دوال للتواصل مع الـ Backend
   - معالجة الأخطاء

3. **ملفات التكوين**
   - `.env` - متغيرات البيئة
   - `BACKEND_SETUP.md` - شرح كامل

---

### ⚙️ خطوات التشغيل (أول مرة):

#### 1️⃣ تثبيت المتطلبات

**Backend:**
```bash
cd server
npm install
```

**الرجوع للمجلد الرئيسي:**
```bash
cd ..
```

#### 2️⃣ تشغيل التطبيق

**في Terminal أول:**
```bash
npm run dev
```

**في Terminal ثاني:**
```bash
npm run server
```

---

### 📊 النتيجة:

✅ Frontend يعمل على: `http://localhost:5173`
✅ Backend يعمل على: `http://localhost:5000`
✅ البيانات تُحفظ في: `server/data/mosqee.db`
✅ جميع البيانات ستبقى محفوظة حتى بعد إغلاق الصفحة! 🎉

---

### 🔍 اختبر الاتصال:

1. افتح التطبيق على `http://localhost:5173`
2. سجل حسابك جديد
3. أضف درس أو منشور
4. أغلق الصفحة والبيانات ستبقى موجودة! ✨

---

### 📚 للمزيد من المعلومات:

اقرأ:
- `BACKEND_SETUP.md` - شرح كامل للـ Backend
- `server/README.md` - شرح الخادم
- `DATA_PERSISTENCE_FIX.md` - تحسينات الحفظ السابقة
